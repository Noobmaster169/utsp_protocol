import { useAuth } from './useAuth';

function VotingParticipants({canister}){
    const {tokenManager} = useAuth();

    
    return{
        
    }



}

export default VotingParticipants;